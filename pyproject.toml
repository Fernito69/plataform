[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["I", "F401"]

[tool.ruff.lint.isort]
known-first-party = [
    "entities",
    "factories",
    "mappings",
    "model",
    "three_d_renderer",
]
